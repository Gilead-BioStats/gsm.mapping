# Mapping Specifications

## Mapping Workflows

Currently, there exist **23 domains** that have respective workflows
that require mapping from source/raw data. Each domain generally maps
directly 1-to-1 from a raw source, except `COUNTRY` which relies on the
same dataset as `SUBJ`. The following variables/types from their
respective domains is **REQUIRED** in the source/raw data:

#### All Domain Mappings
