# Mapping Specifications

## Mapping Workflows

Currently, there exist **23 domains** that have mapping specifications
for transforming raw/source data into standardized formats for GSM
workflows. Each mapping specification defines the required variables,
their data types, and transformation rules needed to create consistent,
analysis-ready datasets.

The table below shows all available domain mappings with their
specifications. Use the search and filter functionality to find specific
domains or variable types. Hover over truncated descriptions and dataset
names to see the full text.

#### All Domain Mappings
