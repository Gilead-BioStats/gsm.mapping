% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete_non_starter.R
\name{complete_non_starter}
\alias{complete_non_starter}
\title{Classify IP non-starter status per enrolled subject}
\usage{
complete_non_starter(
  dfSubjects,
  dfStudyDrugCompletion,
  dfStudyCompletion,
  nWindowDays = 30,
  chrNeverDosedReason = "Subject Never Dosed with Study Drug"
)
}
\arguments{
\item{dfSubjects}{\code{Mapped_SUBJ}: \verb{studyid, subjid, invid, country, firstdosedate, timeonstudy}.}

\item{dfStudyDrugCompletion}{\code{Mapped_SDRGCOMP}: \verb{studyid, subjid, sdrgreas}.}

\item{dfStudyCompletion}{\code{Mapped_STUDCOMP}: \verb{studyid, subjid, compreas}.}

\item{nWindowDays}{\code{integer} window in days separating the two potential
statuses; default \code{30}.}

\item{chrNeverDosedReason}{\code{character} coded \code{sdrgreas} value marking a
confirmed non-starter, matched ignoring case; default
\code{"Subject Never Dosed with Study Drug"}.}
}
\value{
a \code{data.frame} with one row per enrolled subject and columns
\verb{studyid, subjid, invid, country, dosed, nonstarter_status, confirmed_nonstarter}.
}
\description{
Joins the mapped subject, study-drug-completion and study-completion frames
to classify each enrolled subject's IP non-starter status. \code{Mapped_SUBJ} is
already filtered to \code{enrollyn == 'Y'}, so every row is an enrolled subject.
}
\details{
A subject is a \strong{confirmed} non-starter when they are enrolled, not dosed
(\code{firstdosedate} is \code{NA} or blank) and have either a non-blank study-completion reason
(\code{compreas}) or the coded study-drug-completion reason (\code{sdrgreas} matching
\code{chrNeverDosedReason}, compared case-insensitively).

\code{nonstarter_status} is assigned by explicit predicate: \code{"Started"} when
dosed, \code{"Confirmed"} when a confirmed non-starter, \code{"Potential-within"} /
\code{"Potential-outside"} for the remaining subjects whose \code{timeonstudy} falls at
or below vs. above \code{nWindowDays}, and \code{NA} when none apply (e.g. \code{timeonstudy}
is missing so the window cannot be evaluated).
}
\examples{
\dontrun{
lMapping <- workr::MakeWorkflowList(
  strPath = "workflow/1_mappings",
  strNames = c("SUBJ", "SDRGCOMP", "STUDCOMP"),
  strPackage = "gsm.mapping"
)
lRaw <- gsm.mapping::Ingest(lSource, gsm.mapping::CombineSpecs(lMapping))
mapped <- workr::RunWorkflows(lMapping, lRaw)
complete_non_starter(
  dfSubjects = mapped$Mapped_SUBJ,
  dfStudyDrugCompletion = mapped$Mapped_SDRGCOMP,
  dfStudyCompletion = mapped$Mapped_STUDCOMP
)
}
}
